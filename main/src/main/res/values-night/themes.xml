<resources>
    <!-- Базовая тема для Compose - минимальная, так как вся стилизация будет в коде Compose -->
    <style name="Theme.Draft" parent="android:ThemeOverlay.Material.Dark.ActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/purple_500</item>
    </style>
</resources>